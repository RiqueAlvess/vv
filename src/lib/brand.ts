export const brand = {
  logoUrl: process.env.NEXT_PUBLIC_LOGO_URL ?? '/logo.svg',
  authBgUrl: process.env.NEXT_PUBLIC_AUTH_BG_IMAGE_URL ?? '',
  name: 'Asta', // used only for alt text and page titles, never rendered as visible text
};
